name: job1
on:
  workflow_dispatch:
  
jobs:
  testjob:
    uses: ./job2.yml@main

  checkJob:
    if: ${{ always() && ! ${{ skipped() || cancelled() }} }}
    needs: testjob
    uses: ./job2.yml@main
